package certification;
// package com.cert;     // won't compile (two package aren't allowed)

public class ExamQuestion {
  // package com.cert;     // won't compile 
  
}
// package com.cert;     // won't compile
